flask>=3.0
python-docx>=1.1
pypdf>=4.0
pandas>=2.0
openpyxl>=3.1
pyyaml>=6.0
rapidfuzz>=3.6
anthropic>=0.40
python-dotenv>=1.0

# --- OCR para PDFs ESCANEADOS (opcional) ---
# Além destes pacotes, instale os programas do sistema:
#   Tesseract (com idioma português) e Poppler.
# Windows:  https://github.com/UB-Mannheim/tesseract/wiki  e  poppler-windows
# Sem isso, PDFs escaneados são marcados como "pdf-sem-texto".
pytesseract>=0.3.10
pdf2image>=1.17
